streamlit
folium
streamlit-folium

# força ferramentas de build e pkg_resources (evita surpresas)
setuptools==69.5.1
wheel

# base numérica compatível
numpy==1.26.4

# geometrias (wheels prontos p/ py311)
shapely==2.0.3

# projeção (wheels prontos com PROJ embutido em geral)
pyproj==3.6.1

# caso use supabase
supabase
python-dotenv
requests
fpdf2==2.8.3

# html -> pdf
# trava pydyf para evitar quebra de compatibilidade com WeasyPrint 60.1
weasyprint==60.1
pydyf==0.10.0
cffi>=1.15
tinycss2>=1.2
cssselect2>=0.7

# snapshot visual -> imagem -> PDF
Pillow>=10.4.0
